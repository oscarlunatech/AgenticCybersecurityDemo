# --- Secrets / credentials -------------------------------------------------
.env
*.env
!.env.example
*.pem
*.key
credentials

# --- Terraform -------------------------------------------------------------
*.tfstate
*.tfstate.*
.terraform/
crash.log
crash.*.log
*.tfvars
*.tfvars.json
!*.tfvars.example
override.tf
override.tf.json
# .terraform.lock.hcl is intentionally NOT ignored — commit it.

# --- Node ------------------------------------------------------------------
node_modules/
npm-debug.log*
# package-lock.json is intentionally NOT ignored — commit it (CI runs `npm ci`).

# --- Playwright (browser e2e) ----------------------------------------------
test-results/
playwright-report/
blob-report/
playwright/.cache/

# --- OS / editor -----------------------------------------------------------
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
.claude/
CLAUDE.md
ChallengeCreation.md
