{
  "name": "demo-orchestrator",
  "version": "0.2.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "test:integration": "node --test test-integration/session-lifecycle.js",
    "integration:images": "docker build -t lab-sqli-login:latest ../targets/sqli-login && docker build -t lab-idor-invoices:latest ../targets/idor-invoices && docker build -t lab-blind-sqli:latest ../targets/blind-sqli && docker build -t lab-client:latest ../client-image"
  },
  "dependencies": {
    "dockerode": "^4.0.2",
    "express": "^4.19.2",
    "http-proxy": "^1.18.1",
    "ws": "^8.18.0"
  }
}
